apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastfood-service-deployment
  namespace: tcc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastfood-service
  template:
    metadata:
      labels:
        app: fastfood-service
    spec:
      containers:
        - name: fastfood-service
          image: felixgilioli/fastfood-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: MERCADO_PAGO_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: fastfood-service-secret
                  key: MERCADO_PAGO_ACCESS_TOKEN
          resources:
            requests:
              memory: "400Mi"
              cpu: "125m"
            limits:
              memory: "600Mi"
              cpu: "400m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5